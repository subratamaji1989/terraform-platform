# Local Terraform directories and files
**/.terraform/*
*.tfstate
*.tfstate.*
crash.log
crash.*.log
*.tfplan
*.lock.hcl
precheck.json

# In this project, the `generated` directory is used for pipeline-created
# variable files, which should not be committed.
/generated/

# Exclude all .tfvars files, which are likely to contain sensitive data, such as
# passwords, private keys, and other secrets. These should not be part of version
# control as they are data-specific.
*.tfvars
*.tfvars.json

# Ignore override files as they are usually used for local testing
*.override.tf
*.override.tf.json
*_override.tf
*_override.tf.json

# Ignore CLI configuration files
.terraformrc
terraform.rc

# IDE / Editor directories
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# OS-generated files
#
# Mac
.DS_Store
.AppleDouble
.LSOverride

# Windows
Thumbs.db
ehthumbs.db